# Local Recall

Local-first, encrypted desktop activity recall with explicit capture controls and optional remote AI providers.

Development is tracked through GitHub issues. The authoritative product requirements will live in `docs/requirements.md`.

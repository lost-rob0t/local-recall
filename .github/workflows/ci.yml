name: CI

on:
  push:
    branches: [main]
  pull_request:

permissions:
  contents: read

concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  checks:
    name: Checks (${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-22.04, ubuntu-24.04]
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.14"
      - uses: astral-sh/setup-uv@v6
        with:
          enable-cache: true
      - name: Install locked dependencies
        run: ./scripts/bootstrap
      - name: Check formatting
        run: uv run --no-sync ruff format --check .
      - name: Check pipeline source lint
        run: uv run --no-sync ruff check src/local_recall/pipeline
      - name: Check pipeline configuration lint
        run: uv run --no-sync ruff check src/local_recall/config tests/unit/config/test_pipeline_settings.py
      - name: Check pipeline unit lint
        run: uv run --no-sync ruff check tests/unit/pipeline
      - name: Check pipeline integration lint
        run: |
          python - <<'PY'
          from pathlib import Path
          import subprocess

          completed = subprocess.run(
              ["uv", "run", "--no-sync", "ruff", "check", "tests/integration/pipeline"],
              capture_output=True,
              text=True,
              check=False,
          )
          output = completed.stdout + completed.stderr
          Path("ruff-integration.txt").write_text(output, encoding="utf-8")
          print(output, end="")
          raise SystemExit(completed.returncode)
          PY
      - name: Upload integration lint diagnostics
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: ruff-integration-${{ matrix.os }}
          path: ruff-integration.txt
          if-no-files-found: error
      - name: Check pipeline architecture lint
        run: uv run --no-sync ruff check tests/security/test_pipeline_architecture.py
      - name: Check Python lint
        run: uv run --no-sync ruff check .
      - name: Check shell scripts
        run: uv run --no-sync shellcheck scripts/bootstrap scripts/check scripts/test scripts/run-pytest scripts/security-scan
      - name: Check static types
        run: uv run --no-sync pyright
      - name: Run unit tests
        run: ./scripts/run-pytest tests/unit
      - name: Run contract tests
        run: ./scripts/run-pytest tests/contract
      - name: Run integration tests
        run: ./scripts/run-pytest tests/integration
      - name: Run security tests
        run: ./scripts/run-pytest tests/security
      - name: Run security checks
        run: ./scripts/security-scan

  failure-propagation:
    name: Failure propagation
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.14"
      - uses: astral-sh/setup-uv@v6
      - name: Install locked dependencies
        run: ./scripts/bootstrap
      - name: Verify known failures remain non-zero
        run: ./scripts/verify-failure-modes
